# Copy to .env.local for local development against Supabase. Leave empty for browser-only mode.
# Use the publishable (or legacy anon) key — never the service_role / secret key.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_KEY=sb_publishable_xxxxxxxxxxxxxxxx
